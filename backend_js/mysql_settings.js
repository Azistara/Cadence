//mysqlSettings object should be passed to connect to mysql server/db

let mysqlSettings = {
    host     : 'localhost',
    user     : 'root',
    password : ''
};


module.exports = {mysqlSettings};
